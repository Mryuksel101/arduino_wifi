import 'package:flutter/material.dart';

class PeoplesView extends StatelessWidget {
  const PeoplesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Peoples View"));
  }
}
